name: pull_request-workflow 2
on: 
  pull_request:
    types: [opened]

jobs:
  pull-request-job:
    runs-on: ubuntu-latest
    steps:
      - name: step 1
        run: echo "This is a pull request workflow"
      - name: step 2
        run: |
          echo "This is pull_request workflow - step 2"
          echo "Running multiple commands"
